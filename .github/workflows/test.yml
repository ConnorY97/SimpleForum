name: Test

on:
  workflow_dispatch:
    {}

jobs:
  Test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v4

      - name: Create Build Directory
        shell: bash
        run: mkdir build

      - name: Enable Testing
        shell: bash
        run: cmake -S . -B build -DBUILD_TESTS=ON

      - name: Compile
        working-directory: build
        run: cmake --build .

      - name: Test
        working-directory: build
        run: |
          ./tests/test_login --reporter junit --out test_login.xml
          ./tests/test_forums --reporter junit --out test_forums.xml
          ./tests/test_comments --reporter junit --out test_comments.xml

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Test Results
          path: build/*.xml

      - name: Upload Results
        uses: dorny/test-reporter@v2
        with:
            fail-on-empty: "false"
            name: Test Results
            path: "build/*.xml"
            reporter: java-junit
